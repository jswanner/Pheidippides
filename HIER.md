# Directory structure

```
.
├── app               # Exposure of application to web, via Rails
│   ├── controllers
│   └── views
└── lib
    └── pheidippides  # Core of the application, think of what would be useful if in a gem
        ├── models    # Behaviour / business logic
        └── stores    # Persistence

```
